git clone linkhere.com
git commit -m "commit"
git push -u origin master
git fetch
git push origin
git merge origin/master
git pull

confilct resolution:
   1)add
   2)commit
   3)fetch
   4)merge
   5)push origin


to update the repository in your machine:

1) git fetch
2) git merge origin/master